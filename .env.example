# Netlify Functions + Resend (server-side — set in Netlify dashboard or .env for `netlify dev`)

RESEND_API_KEY=re_xxxxxxxxxx
ENQUIRY_FROM_EMAIL=Ananta One <noreply@yourdomain.com>
ENQUIRY_TO_EMAIL=hello@anantaone.in

# Optional: override API endpoint (defaults to /api/enquiry)
# VITE_ENQUIRY_API_URL=/api/enquiry

# Local development with functions:
# npx netlify dev
